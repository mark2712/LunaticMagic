# 📖 Localization System Documentation

> **Для кого:** Для меня в 3 часа ночи через 10 лет.
> **Зачем:** Чтобы понять, почему оно так сложно устроено и как добавить перевод для текстуры кота.

## 🚀 TL;DR (Quick Start)

**Инициализация (в точке входа):**

```csharp
// Создаем систему. "en" - стартовый язык.
GlobalGame.Localization = new LocalizationState("en");

```

**Использование (в UI):**

```csharp
// 1. Получаем байндинг (это не просто строка, это ссылка на ресурс!)
var binding = GlobalGame.Localization.Text.Bind("ui.main_menu.title");

// 2. Выводим текст
myLabel.text = binding.Text; 

// 3. (Важно!) Освобождаем, когда окно закрывается
binding.Dispose(); 

```

---

## 🏗 Архитектура: "Зачем так сложно?"

Мы разделили ответственность на три части, чтобы система была гибкой и не грузила память зря.

### 1. 🧠 LocalizationState (Мозг)

* **Что делает:** Хранит **только** текущий язык (`ReactiveProperty<string>`) и список доступных языков.
* **Почему отдельно:** Потому что знать "какой сейчас язык" нужно всем (звуку, текстурам, аналитике), а вот грузить JSON-файлы с текстом нужно только текстовому сервису.
* **Где живет:** `GlobalGame.Localization` (реализует `ILocalization`).

### 2. 📚 TextLocalizationService (Библиотекарь)

* **Что делает:** * Следит за сменой языка.
* Загружает JSON файлы (модули) с диска.
* **Hot-Swap:** Когда меняется язык, он не выгружает модули, а просто подменяет внутри них словари данных (Memory Replacement).


* **Модульность:** Текст разбит на файлы (`ui.json`, `items.json`). Мы не грузим весь текст игры в память сразу.

### 3. 🎫 LocalizationBinding (Читательский билет)

* **Что делает:** Это "контракт" на использование конкретного ключа.
* **RefCounting:** Пока жив хоть один Binding на модуль `ui`, этот модуль висит в памяти. Когда все Binding'и вызывают `Dispose()`, модуль выгружается.
* **Reactivity:** Свойство `.Text` — это геттер. Оно лезет в сервис и берет **актуальный** перевод прямо сейчас.

---

## 📂 Файловая структура

Путь: `DataPathManager.Localization`

```text
Localization/
├── en/
│   ├── ui.json       <-- Модуль "ui"
│   └── items.json    <-- Модуль "items"
├── ru/
│   ├── ui.json
│   └── items.json

```

**Ключи:** `ui.main_menu.title` автоматически понимает, что искать надо в файле `ui.json`. Если точки нет (`hello_world`), ищется в `common.json`.

---

## 🛠 API Reference

### `ILocalization` (Главный фасад)

* `Language` *(ReactiveProperty)*: Подпишись, если хочешь знать, когда сменили язык.
* `SetLanguage(string lang)`: Сменить язык ("ru" -> "en"). Перезагрузка данных произойдет автоматически.
* `GetLanguageList()`: Сканирует папки и говорит, какие языки есть.
* `Text`: Доступ к текстовому сервису.

### `ITextLocalization` (Сервис текста)

* `Bind(string key)`: Возвращает `ILocalizationBinding`. **Внимание:** Этот метод увеличивает счетчик ссылок на модуль! Обязательно вызывать `Dispose()` у результата.

### `ILocalizationBinding`

* `Text`: Возвращает строку на текущем языке.
* `Dispose()`: Говорит системе "мне этот текст больше не нужен". Уменьшает счетчик ссылок.

---

## 🔄 Как работает Реактивность (UIComponent)

В твоем базовом классе `UIComponent` есть нюанс. Сам по себе `Binding` **не шлет событий**, что текст изменился. Он просто всегда отдает актуальный текст.

Чтобы UI обновился "на лету", компонент должен перерисоваться при смене языка.

**Правильный паттерн в UI:**

1. Подписываемся на `GlobalGame.Localization.Language`.
2. При срабатывании вызываем метод перерисовки (например `UpdateVisuals()`).
3. Внутри `UpdateVisuals()` просто берем `binding.Text`.

*Пример из твоего кода:*

```csharp
// В Init() компонента:
Use(GlobalGame.Localization.Language); // Если ваш фреймворк поддерживает Use

// Или вручную:
GlobalGame.Localization.Language.Subscribe(_ => RefreshText());

// В коде отрисовки:
label.text = Text("ui.menu.title"); // Внутри вызовется binding.Text и вернет новую строку

```

---

## 🔮 Future Proofing: Как расширять?

### "Хочу локализацию Текстур/Картинок!"

1. Создай интерфейс `ITextureLocalization`.
2. Создай сервис `TextureLocalizationService`.
* В конструктор передай `ILocalization` (чтобы знать текущий язык).
* Логика загрузки: `Path.Combine(path, CurrentLanguage, textureName)`.


3. Добавь это в `LocalizationState`:

```csharp
public class LocalizationState : ILocalization 
{
    // ... старое ...
    public ITextureLocalization Textures { get; } // <--- Новое

    public LocalizationState(...) {
        // ...
        Textures = new TextureLocalizationService(this);
    }
}

```

### "Хочу Аудио!"

Аналогично текстурам. Создай `IAudioLocalization`. Аудио обычно тяжелое, поэтому там RefCounting (как в тексте) будет критически важен.

---

## ⚠️ Частые ошибки (Troubleshooting)

1. **Забыл Dispose:** Модуль локализации навсегда останется в памяти. Если много открывать/закрывать разные окна — утечка памяти.
2. **Забыл создать файл:** Вернется строка `!key!`.
3. **Не тот JSON:** Если ключ `items.sword`, а файл `ui.json`, система будет искать файл `items.json`.
4. **NullReference при старте:** Убедись, что `GlobalGame.Localization` инициализирован **до** того, как UI пытается что-то биндить.
